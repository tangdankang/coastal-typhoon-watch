# 浙江台风数据接入核验记录

日期：2026-08-31。

1. 确认新官方入口 `https://typhoon.slt.zj.gov.cn/`；旧 `typhoon.zjwater.gov.cn` 不作为生产接口。
2. 读取官方首页引用的 `js/app~42f9d7e6.45636167.js`，确认 `/Api` 基地址及 `TyhoonActivity`、`TyphoonList`、`TyphoonInfo` 路由。没有使用第三方镜像、认证凭据或签名破解。
3. 当次活动列表为真正 JSON 空数组；2026 年年度列表返回多个非活动台风，最近记录包含 202623 班朗等。读取其详情验证字段：speed=风速、movespeed=移速、time=北京时间。该次返回仅作接口核验，不在文档中宣称持续实时事实。
4. 来源站没有提供本地有效预警完整生命周期；实况页不迁移演示页中的虚构分区预警。页面提供官方原文入口和未知状态。
5. GitHub 初始返回私有仓库套餐不支持 Pages（HTTP 422）；用户明确同意公开后才修改仓库可见性，并以 workflow 类型启用 Pages。
6. 发布路径按 [GitHub Pages 自定义工作流文档](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages) 配置；计划调度遵循 [GitHub Actions schedule 文档](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule)。
7. 自然地球陆地数据沿用原项目，来源与公共领域条款：https://www.naturalearthdata.com/about/terms-of-use/ 。不复制官方系统地图瓦片、徽标或第三方底图。
8. 正式授权尚未取得，不把公开网站端点描述为已获授权的商业 API，也不声称本站是官方服务。维护边界见总结性说明。
9. 首次 Actions 运行 `33372585907` 结论为 success；公开网站、资源和 JSON 检查均成功。15 项测试、类型检查、限定范围 lint 与生产构建通过。未进行浏览器操作式 QA。只将本地开发进程用于预览，线上自动更新由 GitHub Actions 执行，不依赖本机保持开启。
10. 2026-09-02 核查到定时工作流连续 9 次失败，均停在活动列表解析；当天活动端点返回沙德尔、科罗旺两个对象，但该端点本来就不含年度列表使用的 `isactive/endtime`。调整为分别校验两种公开响应结构，并增加活动对象、缺字段和重复 ID 测试；不放松坐标、时间、状态一致性或详情校验。
11. 为只停止本项目的重复失败邮件、且不影响其他仓库的 GitHub 通知，定时运行使用容错：任何步骤失败时不部署、不覆盖旧站，并让工作流整体不触发失败邮件；失败步骤仍留在 Actions 日志。push 和手动运行不容错。项目仓库当前通知状态已是 Participating，故未改全账号 Actions 通知。
